# Presente!

O arquivo de contexto deste projeto é o `AGENTS.md`, na raiz. Leia-o antes de qualquer trabalho e siga o que ele manda: onde o trabalho vive, o que fazer quando uma decisão fecha, como o diário de bordo funciona, o padrão editorial e o que nunca pode ser commitado.

Regra que vale para toda IA entra no `AGENTS.md`, não aqui. Este arquivo existe só porque o Claude Code não lê o `AGENTS.md` por conta própria.
